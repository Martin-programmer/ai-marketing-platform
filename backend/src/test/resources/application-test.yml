spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/amp_test
    username: amp_test
    password: amp_test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  cache:
    type: none

app:
  security:
    dev-mode: true
    jwt-secret: test-secret-key-that-is-at-least-32-bytes-long!!
